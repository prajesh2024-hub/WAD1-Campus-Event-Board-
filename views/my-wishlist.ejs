<%- include('partials/header') %>
<%- include('partials/navbar') %>

<h2>My Wishlist</h2>

<% if (!collections || collections.length === 0) { %>
  <div class="message">
    <p>You have not yet created any wishlist collections.</p>
    <p><a href="/all-events">Browse events</a> and add some to your wishlist.</p>
  </div>
<% } %>

<% collections.forEach(collection => { %>
  <div class="event-card" style="margin-bottom: 20px;">
    <form action="/my-wishlist/collections/<%= collection._id %>/edit" method="POST" style="display:flex; gap:10px; align-items:center; margin-bottom:10px;">
      <input type="text" name="collectionName" value="<%= collection.name %>" style="padding:8px; width: 250px;" required />
      <button type="submit" class="btn-primary">Update Collection Name</button>
    </form>

    <% if (!collection.items || collection.items.length === 0) { %>
      <p>This collection is empty.</p>
    <% } else { %>
      <table class="my-events-table" style="margin-top:8px;">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Date</th>
            <th>Venue</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% collection.items.forEach(item => { %>
            <% const event = item.event; %>
            <% if (!event) return; %>
            <tr>
              <td>
                <form action="/my-wishlist/collections/<%= collection._id %>/events/<%= event._id %>/remove" method="POST" style="display:inline;">
                  <button type="submit" title="Remove from collection" class="star-button">★</button>
                </form>
              </td>
              <td><a href="/events/<%= event._id %>"><%= event.title %></a></td>
              <td><%= new Date(event.startDate).toLocaleDateString('en-US', { day: '2-digit', month: 'short', year: 'numeric' }) %></td>
              <td><%= event.venue %></td>
              <td class="action-links">
                <form action="/events/<%= event._id %>/rsvp" method="POST">
                  <button type="submit">Join Event</button>
                </form>

                <form action="/my-wishlist/collections/<%= collection._id %>/events/<%= event._id %>/move" method="POST" style="display:inline-block;">
                  <select name="targetCollectionId" required>
                    <% collections.forEach(target => { %>
                      <% if (target._id.toString() !== collection._id.toString()) { %>
                        <option value="<%= target._id %>"><%= target.name %></option>
                      <% } %>
                    <% }); %>
                  </select>
                  <% if (collections.length > 1) { %>
                    <button type="submit">Change collection</button>
                  <% } %>
                </form>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } %>
  </div>
<% }); %>

<%- include('partials/footer') %>